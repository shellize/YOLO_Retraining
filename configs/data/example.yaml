# Legacy catalog example: one logical group owns train/val/test directories.
# New batch-based experiments should use self_improving.yaml instead.
path: ../../datasets/stage0
train: images/train
val: images/val
test: images/test
names:
  0: object
