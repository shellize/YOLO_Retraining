include: [../defaults.yaml, ../model/yolov5s.yaml]
task:
  label: full-warm
  parent_result: null
data:
  layout: ../data/self_improving.yaml
  current: [stage0]
  candidate: [stage0]
  validation: [val]
  test: [test, test_filtered]
initialization: {source: parent, checkpoint: last}
select_policy: {name: full, params: {}}
