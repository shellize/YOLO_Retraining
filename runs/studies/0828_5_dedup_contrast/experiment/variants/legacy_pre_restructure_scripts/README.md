# Legacy pre-restructure launchers

These files are exact copies of the launchers that remained in the server-side
`runs/studies/balance_tau090_label_contrast/` directory before the 2026-09-08
cleanup.

They are retained only as protocol provenance. Their embedded paths refer to
the former directory layout and they must not be used to start a new run. Use
the current launchers in this Study's `scripts/` directory instead.
