# Legacy pre-restructure launcher

This file is an exact copy of the launcher that remained in the server-side
`runs/studies/global_dedup_tau099_learning_curve/` directory before the
2026-09-08 cleanup.

It is retained only as protocol provenance. Its embedded paths refer to the
former directory layout and it must not be used to start a new run. Use the
current launchers in this Study's `scripts/` directory instead.
